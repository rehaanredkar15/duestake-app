

// eslint-disable-next-line import/no-anonymous-default-export
const LoginReducer =  (userDetails = [] , action) => {

   switch (action.type) {
    

    default:
      return userDetails;
  }
};

export default LoginReducer;